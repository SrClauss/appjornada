import 'package:flutter/material.dart';

class JornadaDetalheScreen extends StatelessWidget {
  const JornadaDetalheScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Em construção')),
      body: const Center(child: Text('TODO: Fase 3')),
    );
  }
}
